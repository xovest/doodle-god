#include "header.h"

int main() {
  string word = "Node *clothes();";
  word = word.substr(6);
  cout << word;
  Node* root = clothes();
  // cout << maxDepth(root);
  // print2D(firearm());
  return 0;
}
